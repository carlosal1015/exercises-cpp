#include <iostream>
// input/output stream (flujo)

// Especificamos un namespace
int main() {
  // definicion de variables
  int a;
  float z1;
  double var3;

  // Imprimir en pantalla
  std::cout << "Este es mi primer mensaje en C++" << std::endl;

  // Imprimamos otro mensaje en pantalla
  std::cout << "Sumemos a + z1 + varf3 = " << a + z1 + var3 << std::endl;

  return 666;
}